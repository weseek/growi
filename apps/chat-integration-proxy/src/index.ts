// Entry point scaffold for @growi/chat-integration-proxy (Gen 2 chat-integration hub).
// This task (1.1) only sets up the app's build/test/lint pipeline and the
// exact-pinned Chat SDK dependencies. The real layered structure
// (types / capabilities / db / platform / command / relation / growi /
// orchestration / routes / runtime) is added incrementally by later tasks in
// .kiro/specs/chat-integration-proxy/tasks.md.
export const CHAT_INTEGRATION_PROXY_PACKAGE_NAME =
  '@growi/chat-integration-proxy';
