# Implementation Plan — chat-integration-app

> umbrella spec: [chat-integration](../chat-integration/)。要件は umbrella の `requirements.md` が持つ。
> **本 spec が担当する要件 ID は 50 件**（85 件は 3 つの sub-spec で分担する）。

**`chat-integration-protocol` の完了が前提。** 本 spec は `@growi/chat` の型と関数に依存する。

**Gen 1（`apps/slackbot-proxy` と `packages/slack`）には手を入れない。** 読むだけ。

**feature の外を触るタスクは 7 つある** — 1.1（依存と build の順序）、第 2 章（全体に掛かる 3 つの仕組み）、
3.5（受け口を既存の経路に登録する）、6.1（承認画面の経路）、6.2（個人設定のタブ）、
8.2（定期実行の起動）、9.1（管理画面の受け皿と案内の一覧）。
**それ以外は `features/chat-integration/` の中だけで完結する。**

**各タスクは自分のファイルから直接出す。層の入口はその層の最後のタスクが作る。**
そうしないと、同じ層で並行して進むタスクが 1 つの入口ファイルを取り合う。

---

- [ ] 1. feature の土台を作る
- [x] 1.1 feature の骨組みと依存を用意する
  - `apps/app/src/features/chat-integration/` を作り、`@growi/chat` を依存に足す
  - **build の順序を宣言する**（同じリポジトリ内の build を作る依存が増えたら書き足す決まり）
  - **feature の入口を作り、そこから起動時に辿れるようにする**（次のタスクで置く model が
    読み込まれる経路になる）
  - 型検査と build が通り、feature の入口が起動経路から辿れる
  - _Requirements: 1.3_

- [x] 1.2 新しいデータの置き場所を作る
  - **11 個のコレクション**を宣言する — 関係・紐付け・鍵・通知の控え・処理済みの記録・
    使い捨ての値・保留中のペアリング・一度きりのリンク・所有確認の回数・
    チャンネル権限・通知の宛先
  - **関係の識別子は単独で一意にする**（相手が採番する値だが、届くリクエストがこれだけで
    関係を名乗るので、引き当てが一意にならない形にはできない）
  - 紐付けは関係・サービス・アカウントの組で**複合ユニーク**にする（要件 7.4 の実現手段）
  - 送信待ちの控えには**奪った時刻**の列と、2 つの索引（奪うときに引く組・結果を書き戻すときに引く組）
  - **model は feature の入口から静的に読み込む**（読み込まれた瞬間に索引が作られるので、
    辿れない置き方だと機能を最初に使うまで索引ができない）
  - 起動すると 11 個のコレクションと索引が実際に作られる
  - _Requirements: 7.4, 10.4_
  - _Depends: 1.1_

- [x] 1.3 秘密鍵を暗号化して保存する仕組みを作る
  - **GROWI に前例が無いので、仕組みごと作る**（設定を伏せ字にする既存の仕組みは暗号化ではない）
  - 暗号化に使う鍵は**環境変数から読む**。設定画面からは入れられないようにする
    （保存に入れると、保存を取られた人が暗号化した鍵も一緒に取れて意味が無くなる）
  - **未設定ならペアリングを始められない。平文で保存に落とさない**
  - 復号は署名する関数の中だけで行い、他の層へは復号した値を渡さない
  - 入れ替えのために、行に**どの世代の鍵で暗号化したか**を持たせる
  - 暗号化した列が保存の上では読めず、環境変数が無いとペアリングが始められないことが試験で示される
  - _Requirements: 10.6_
  - _Depends: 1.2_

- [ ] 2. 全体に掛かる仕組みに手を入れる
- [x] 2.1 proxy から届く口だけ、本文を生のバイト列で受ける
  - **全体の JSON 解析より前に**、proxy 向けの口の接頭辞だけ生のバイト列で受ける
  - **掛ける範囲を接頭辞で絞る。**同じ feature の中には管理画面が叩く口もあり、
    そちらまで生のバイト列になると入力検査ごと動かなくなる
  - **`Buffer` のまま扱う**（文字列に足し込むと、複数バイトの文字が塊の境目で壊れる）
  - 種類が JSON でなければ、署名の検証に入る前に断る
  - 日本語を含む本文を送って、口に届いた時点でバイト列が送信時と一致し、
    **同じ接頭辞の下の管理画面用の口は解析済みのまま**であることが試験で示される
  - _Requirements: 10.1_
  - _Depends: 1.1_

- [x] 2.2 proxy 向けの口を、消毒と session の仕組みから外す
  - **保存の値を消毒する仕組みから外す。** その仕組みは `Buffer` を素の object とみなすので、
    全バイトぶんの添字を列挙して 1 件ずつ判定を回す（実測で 1 MiB あたり 0.13 秒、
    上限に決めた大きさでは 1 秒を超える）。**`Buffer` に保存の演算子は入りようがないので、掛ける意味も無い**
  - **session を張る仕組みから外す。** 全体の設定は「一度も触られていない session も保存する」形なので、
    要求 1 本ごとに保存へ文書が 1 件書かれる。とくに**署名の要らない所有確認の口**では、
    回数の上限に達して断る要求も、断る前に文書を 1 件書く
  - 消毒の仕組みは条件無しで登録されているので、包む形で外す
  - proxy 向けの口へ大きな本文を送っても消毒に時間を取られず、session の文書が増えないことが試験で示される
  - _Requirements: 10.1_
  - _Depends: 2.1_

- [x] 2.3 Gen 2 の通知の宛先を、既存の送信処理の外に足す
  - 既存の 2 つの宛先の呼び方には**手を入れない**。Gen 2 の宛先を外側に足す
  - **種類で分岐しない形にするのは Gen 2 側だけ。** 宛先の集合を回す形にする
  - ページの保存時に宛先を指定する経路で、Gen 1 の項目を残したまま Gen 2 の宛先を別項目で受ける
  - パス条件の突き合わせに使う既存の処理を外へ出して共有する。**振る舞いは変えない**
  - Slack が未設定でも Gen 2 の宛先だけで成立するようにする
  - **完了の条件は「Gen 2 の宛先が集合として呼ばれること」まで**（受け側は仮のものでよい）。
    実際に両方へ通知が出ることは 8.1 で確かめる
  - _Requirements: 12.1, 12.2, 12.3_
  - _Depends: 1.1_

- [ ] 3. proxy から届くリクエストを受ける
- [x] 3.1 鍵を保持する
  - 自分の鍵と相手の公開鍵を保持する。**鍵の識別子は関係・側・鍵名の組**で扱う
  - 引くときは**相手側の鍵だけを返す**（自分の鍵を引くと、自分が署名した要求を
    自分の口へ差し戻す形が通る）。失効済みとまだ有効でない鍵は引けない
  - **ペアリングの途中の鍵は保留の行に持つ**（関係がまだ無いので、鍵の置き場所が先に要らない形にする）
  - 署名するときは、**復号した値ではなく署名する手段**を上の層へ渡す
  - 関係と鍵名の組で正しい鍵が引け、自分の鍵が相手側として引けないことが試験で示される
  - _Requirements: 9.5, 10.6_
  - _Depends: 1.3_
  - _Boundary: KeyStore_

- [x] 3.2 届くリクエストの署名を検証し、封筒を突き合わせる
  - **口を作る作業より先に置く**（後にすると口ごとに実装者が各自で決めることになる）
  - **本文は生のバイト列のまま検証に渡す。**解析した値から組み立て直したものを渡さない
    （鍵の並び順が送信時と一致する保証が無く、正しい相手が弾かれる）
  - **使い捨ての値に渡す期限は受ける側が上限で切った値**にし、**検証に成功した後にだけ**消費する
  - **検証の後、契約側の検査関数で本文の形を確かめ、さらに本体の関係の識別子と
    口の名前を突き合わせてから処理へ渡す。** 落としても正常な使い方はそのまま動くので、
    後から気づけない
  - 失敗の種類を記録する。**署名そのものと本文は記録しない**
  - **鍵の並び順だけが違う同じ内容を送ると通らない**こと、**別の口へ宛てた要求が断られる**ことが
    試験で示される
  - _Requirements: 10.1, 10.2, 10.3, 10.4, 10.7_
  - _Depends: 2.1, 3.1_
  - _Boundary: SignatureGuard_

- [x] 3.3 操作した人を解決する
  - チャットのアカウントから GROWI ユーザーと所属グループを引く。**関係も鍵に含める**
    （チャットのアカウントの識別子だけでは workspace の軸が無い）
  - **利用者の状態が「有効」でなければ、紐付いていない扱いにする。**
    停止・削除扱い・承認待ちの人が、画面からは何もできないのにチャットからだけ操作できる状態を作らない
  - **読み取り専用の利用者は、本人として検索は通し、書き込みだけ断る**
  - **ログインしていない相手に何も見せない設定の GROWI では、紐付いていない利用者に
    公開ページも返さない**（外へ本文を出す口では、既存の実装もこの設定を必ず見ている）
  - 断る理由を 3 つに書き分ける（紐付いていない・読み取り専用・利用者が使えない）。
    **利用者が使えない場合に「紐付けが要る」と返さない**（その人は既に紐付けており、リンクを開いても変わらない）
  - 状態が有効でない 4 種と読み取り専用について、正しい分岐に入ることが試験で示される
  - _Requirements: 3.6, 3.7, 4.3, 4.4, 4.5, 7.5, 7.6_
  - _Depends: 1.2_
  - _Boundary: ResolveActor_

- [x] 3.4 所有確認に答える
  - 保留中の登録コードと一致するときにだけ答える
  - **署名するのは用途を示す接頭辞を連結した文字列**であって、受け取った値そのものではない
    （そのままだと、本番の署名に使う同じ鍵で任意の文字列に署名して返す窓口になる）
  - **保留が生きている間はどの問いかけにも答える。**答えた内容を覚えて次から断る形にしない
    （登録コードを見た第三者が本物より先に叩くだけで、その登録コードでは二度と成立しなくなる）
  - 回数の上限は**送り元ごと**に数える。送り元の決め方は契約側の決まりに従い、
    **区別できない設定のときはその旨を運用者に見せる**
  - 上限に当たったことを運用者が見られる形で残す（**取り直せば直ると気づけるようにするため**）
  - 別の送り元が上限に当たっていても本物の問いかけが通ることが試験で示される
  - _Requirements: 9.2, 9.3_
  - _Depends: 3.1, 3.2_
  - _Boundary: PairingEndpoint_

- [x] 3.5 proxy から届く 6 つの口を既存の経路に登録する
  - コマンド・紐付けの開始・**設定の取り出し**・鍵の追加・鍵の失効・所有の確認
  - **既存の apiv3 の経路一覧に、この feature の router を 1 か所で登録する**
    （他の feature も同じ形で配線されている）。中身は後続のタスクが埋める
  - **設定の取り出しは、押し込みが届かなかったときに proxy が取りに来る保険**である。
    版と設定を返す
  - 署名の要る 5 つが署名の検証を通り、所有の確認だけが通らないことが試験で示される
  - _Requirements: 11.4_
  - _Depends: 3.2, 3.4_
  - _Boundary: routes_

- [ ] 4. GROWI が送り出す中身を組み立てる
- [x] 4.1 (P) この利用者が見てよいページだけを残す
  - **既存の絞り込みの条件を組み立てる処理を、引数 2 つで呼ぶ**
    （既定値のままだと、リンクを知っている人だけが読めるページが混じる）
  - **本文の抜粋を消すだけの既存の処理を写さない。両方向に間違っている** —
    特定ユーザー限定は素通りし、グループ限定は形の取り違えでメンバー本人でも落ちる
  - 判定にはページ本体が要る（検索結果には公開範囲の項目が含まれない）。
    当たった識別子をまとめて 1 回問い合わせる。**共有の関数に絞り込みを足さない**
    （本体の検索結果まで変わる）
  - 求められた件数の 3 倍を取ってから落とし、落とした後に順位を振り直す
  - 5 種類の公開範囲について、紐付いている場合といない場合で落ちるものが正しいことが試験で示される
  - _Requirements: 3.6, 3.7_
  - _Depends: 3.3_
  - _Boundary: ViewerPageFilter_

- [x] 4.2 (P) 誰でも見られるページかを判定する
  - **相手によらない判定**。入力も戻り値も 4.1 とは違うので、部品を分けてある
  - ログインしていない相手に何も見せない設定の GROWI では、
    通知はそのまま出し（宛先は管理者が設定したもの）、**URL の要約はパスだけにする**
  - 5 種類の公開範囲について、相手によらず「誰でも見られる」ものだけが残ることが試験で示される
  - _Requirements: 2.3, 6.3_
  - _Depends: 1.2_
  - _Boundary: PublicPageFilter_

- [x] 4.3 発言の列をページの本文へ組み立てる
  - **投稿者は発言ごとに解決する**（操作した人を解決する処理は 1 人ぶんしか返さない）
  - **まとめて 1 回引く**（発言の数だけ問い合わせると、100 件の取り込みで 100 回引くことになる）
  - 紐付いていない投稿者は、チャット上の表示名をそのまま書く
    （「不明」と書くと、誰の発言か分かるという条件を満たさない）
  - **ページを作る人と、発言の投稿者は別である**。作成者は取り込みを打った人
  - 紐付いている投稿者は GROWI ユーザー名、いない投稿者は表示名になることが試験で示される
  - _Requirements: 5.2, 5.3_
  - _Depends: 3.3_
  - _Boundary: ConversationPage_

- [x] 4.4 検索結果・ページの要約・ヘルプを組み立てる
  - 検索結果を構造化データへ（**整形済みの表示物ではない**。日時は決めた表記で）
  - ページを要約へ（本文の冒頭・更新日時・コメント数）
  - このバージョンが提供するコマンドの一覧を組み立てる
  - 検索結果に非公開ページが混じらず、要約が公開範囲に応じて中身を落とすことが試験で示される
  - _Requirements: 3.9, 6.2, 6.3, 14.2_
  - _Depends: 4.1, 4.2_
  - _Boundary: content_

- [x] 4.5 通知の文面を組み立て、この層の入口をまとめる
  - 6 つの出来事ぶんの文面。**非公開ページを含むかどうかの判断もここで行う**
  - 本文の埋め込みと、更新時の差分の作り方は既存の振る舞いをそのまま残す
  - **この層の入口もここでまとめる**
  - 6 つの出来事それぞれで文面が作られ、非公開ページの本文が落ちることが試験で示される
  - _Requirements: 2.1, 2.2, 2.3_
  - _Depends: 4.2, 4.3, 4.4_
  - _Boundary: NotificationContent_

- [ ] 5. proxy から届くコマンドを処理する
- [x] 5.1 読み取りのコマンドを処理する
  - 検索・URL の要約・ヘルプの 3 種類。**例外を投げず必ず応答を返す**
  - **冒頭でチャンネル権限を判定する**（契約側の関数を呼ぶ）
  - 処理済みの要求には**1 回目の応答をそのまま返す**
  - 3 種類が通しで動き、同じ要求の 2 回目に 1 回目の応答が返ることが試験で示される
  - _Requirements: 3.6, 3.7, 6.2, 6.3, 10.4, 11.3, 14.2_
  - _Depends: 3.5, 3.3, 4.4_
  - _Boundary: CommandEndpoint_

- [x] 5.2 書き込みのコマンドを、権限と重複まで含めて処理する
  - ページの作成と発言の取り込みの 2 種類
  - **「この人がここに書いてよいか」を判定する**（既存のページ作成の処理はそれを見ていない）
  - パスの重複を事前に確かめ、**「パスが重複」と「権限が無い」を区別して返す**
    （既存の例外は汎用のもので、他の失敗と区別できない）
  - 取り込みも書き込みなので、同じ 3 つ（権限・重複・再送）がそのまま要る
  - **取り込む範囲に発言が 1 件も無いときは proxy 側で打ち切られる**ので、ここには届かない
  - 2 種類が通しで動き、権限が無い場合と重複した場合が別の理由で返ることが試験で示される
  - _Requirements: 4.2, 4.5, 4.6, 5.2_
  - _Depends: 5.1, 4.3_
  - _Boundary: CommandEndpoint_

- [x] 5.3 書き込みを監査ログに残す
  - **リクエストの到着時に文脈を組み立てる既存の仕組みは使えない** — その仕組みは到着時に
    操作した人を読むが、この口では操作した人が署名付きの本体の中にあり、その時点では空である。
    そのまま置くと、**行はできるが操作した人の列が空**になる（動かして眺めても壊れて見えない）
  - **操作した人が決まった後に、自分で文脈を組み立てて記録を始める。**
    到着時刻は入口で 1 度取って持ち回る（記録を始める時刻ではない）
  - **記録を始めたら、必ず後始末で終わる。**成功して出来事を出せば消えるが、
    それ以外のすべての道で自分で消す（時間で消える仕組みも古い順に捨てる仕組みも意図的に無いので、
    漏らすとプロセスを再起動するまで戻らない）
  - **断った要求は記録しない。**失敗した書き込みは「試みた」記録として残す
    （応答が成功の形で返るので、既存の失敗検知はこの口では一度も働かない）
  - 書き込みの後に記録が 1 件でき、**操作した人と発言元のチャンネルが残る**ことが試験で示される
  - _Requirements: 4.3_
  - _Depends: 5.2_
  - _Boundary: CommandEndpoint_

- [ ] 6. チャットの利用者と GROWI ユーザーを紐付ける
- [x] 6.1 一度きりのリンクを発行して承認を受ける
  - **既存の同じ形の仕組みに倣う**（乱数で作った値を短時間で失効させ、使ったら失効させる形が 3 つある）
  - **承認画面の経路を既存の経路一覧に登録する。ログイン必須にする**
    （登録しないと末尾の受け皿に拾われ、そこで使われるのはゲストの閲覧を許す緩いほうの確認になる。
    パスワードの再設定の画面は専用の経路を登録してこれを満たしている）
  - **どのチャットアカウントを、どの GROWI ユーザーに結び付けるのかを画面に出す**
    （出さないと利用者が取り違えたまま承認する）。複数の GROWI が紐づくときは workspace も添える
  - 承認したら紐付けを書き、**同じトランザクションで値を失効させる**
  - 既に別の利用者に紐付いていれば複合ユニークが弾き、その旨を返す
  - **再送のたびに新しいリンクを作らない**（保留中のものがあればそれを返す）
  - リンクが 1 度しか使えず、期限を過ぎると使えないことが試験で示される
  - _Requirements: 7.1, 7.2, 7.3, 7.4_
  - _Depends: 3.5, 1.2_
  - _Boundary: ChatAccountLink_

- [ ] 6.2 利用者が自分の紐付けを確認して解除できるようにする
  - 個人設定に、自分に紐付いているチャットアカウントの一覧と、個別の解除を置く
  - **個人設定のタブの一覧に 1 つ足す**（一覧は 1 つの場所で一元管理されているので、
    そこに書かないと画面に出ない）
  - 解除したら行を消す。以降の書き込みは紐付けが要る経路へ落ちる
  - **GROWI ユーザーが使えなくなっても行は消さない**（利用者削除は状態を変えるだけで、
    操作した人を解決する処理が状態を見るので行が残っていても安全。復帰時に紐付け直さずに済む）
  - 一覧が出て、解除した後に書き込みが断られることが試験で示される
  - _Requirements: 7.5, 7.7_
  - _Depends: 6.1_
  - _Boundary: ChatAccountLink_

- [ ] 7. proxy へ送る
- [ ] 7.1 proxy へ送る唯一の口を作る
  - 送るのは 8 つ — 通知・設定の押し込み・鍵の追加と失効・能力の一覧・接続の状態・
    チャンネルの一覧・ペアリングの申し込み。**すべて署名を付けた POST**
    （申し込みだけ署名が無い。鍵がまだ無い段のため）
  - 本体に関係の識別子と口の名前を載せる。送り先は保存した相手の URL を土台にする
  - **受け取った応答は必ず検査関数を通す**（応答に署名は付かないので、形の確かめが唯一の受け入れ条件）
  - 壊れた応答が断られ、8 つの口すべてに送れることが試験で示される
  - _Requirements: 1.3, 1.4, 10.1_
  - _Depends: 3.1_
  - _Boundary: ProxyClient_

- [ ] 7.2 自分の鍵の入れ替えと、相手からの鍵の操作を受ける
  - 自分の鍵の入れ替えでは、新旧が両方有効な期間を置いてから古い鍵を失効させる
  - 相手からの鍵の追加と失効を受ける。**有効な鍵が 0 本になる失効を断る**（契約側の判定を呼ぶ）
  - 新旧どちらの署名でも検証が通る期間があり、0 本になる失効が断られることが試験で示される
  - _Requirements: 10.5, 10.6_
  - _Depends: 7.1, 3.5_
  - _Boundary: KeyStore_

- [ ] 7.3 ペアリングを申し込み、繋ぎ直したときに紐付けを引き継ぐ
  - 管理者が貼った登録コードを保留として持ち、**自分の公開鍵を添えて**申し込む
  - 成立したら、関係・相手の公開鍵・workspace の情報を保存する。
    **既にある関係の識別子が返ってきたら成立させず、管理者に知らせる**
  - **解除では関係の行を消さず、状態だけ変える**（workspace を残さないと繋ぎ直しのときに
    紐付けを引き継げない）。消すのは鍵・チャンネル権限・宛先だけ
  - **繋ぎ直したときに紐付けを引き継ぐ** — 解除済みの行を同じサービスと workspace で探し、
    いちばん新しいものから紐付けを新しい関係の識別子へ移す。ぶつかったら新しいほうを残し、
    古い行を消す。**この作業を落とすと、繋ぎ直しただけで全利用者の紐付けが消える**
  - 申し込みから成立まで往復し、解除して繋ぎ直すと**紐付けが引き継がれる**ことが試験で示される
  - _Requirements: 9.1, 9.4, 9.5, 9.7_
  - _Depends: 7.1, 7.2, 3.4_
  - _Boundary: ProxyClient_

- [ ] 8. 通知を送り届ける
- [ ] 8.1 送るべき通知を書き留める
  - 書き留める契機は 2 つ — 管理者がパス条件ごとに設定した通知と、
    保存時に編集した人が宛先を指定した通知。**どちらも同じ控えに入れる**
  - 要求の識別子は**書き留めるときに 1 度だけ決め、やり直しても変えない**
  - **関係ごとに繰り返して書き留める**（1 件だけ入れて終わる形にしない）
  - **Gen 1 と Gen 2 の両方が有効なとき、それぞれの連携先へ独立に通知が出る**ことが試験で示される
  - _Requirements: 2.1, 2.2, 2.5, 2.6, 12.3_
  - _Depends: 2.3, 4.5, 7.1_
  - _Boundary: NotificationOutbox_

- [ ] 8.2 書き留めた通知を送り、期限切れを掃除する
  - **既存の定期実行の仕組みを継ぎ、既存の起動処理に 1 行足して回し始める**
    （同じ形が 3 つあるので 4 つ目を新しく起こさない。起動はすべて同じ場所で行われている）
  - 条件つき更新で 1 行ずつ奪う。**奪った印には期限を付ける**
    （期限が無いと、送っている途中でプロセスが落ちた行が誰のものでもなくなり、
     やり直しの対象にも諦めの対象にもならない）
  - **やり直しは宛先ごと。**投稿できた宛先は飛ばされ、失敗した宛先だけが再び試される
  - **署名は作り直す**（要求の識別子は据え置き、使い捨ての値と時刻は取り直す）
  - **一覧をまだ取れていないという失敗は、諦めの回数に数えない**（10 分待てば直るので、
    間隔を長くしてやり直す。ふつうの失敗と同じ数え方だと、紐付けた直後の通知が数回で諦めに落ちる）
  - 上限を超えたら諦めとして残し、運用者が確認できるようにする
  - **同じ回で、解除済みの関係と紐付けを 90 日で消す**（同じ「1 台だけが回す」形に乗せる）
  - 1 つの宛先だけ失敗させてやり直すと、**成功した宛先に二重に投稿されない**ことが試験で示される
  - _Requirements: 2.4, 2.5, 2.6, 10.4, 10.7_
  - _Depends: 8.1_
  - _Boundary: NotificationDispatcher_

- [ ] 9. 管理画面と個人設定を作る
- [ ] 9.0 OAuth 導入 URL の発行元（この spec に決定済み）として `state` の発行・照合を実装する
  - **決定済み（2026-09-07、ユーザー確認済み）: 発行元はこの spec（chat-integration-app / 管理画面）。**
    `chat-integration-proxy` 側にタスクを切り出す必要はない。再度この判断をやり直さないこと
  - **design.md「proxy 実装で見つかった未解決の論点」を先に読むこと。** Slack/Discord の
    OAuth 折り返しに CSRF 対策の `state` を発行・検証する処理が、chat-integration・
    chat-integration-protocol・chat-integration-proxy・chat-integration-app のどの spec にも
    無いまま proxy の実装が完了している（proxy は `code` だけで折り返しを受ける）
  - 管理画面に「新しい workspace を接続する」操作を置き、発行した `state` を GROWI 側で
    一時的に保持し、proxy の OAuth 折り返し（`routes/install-routes.ts`）が呼び戻ってきた
    時点で照合する経路を用意する
  - 実装後、この spec の design.md の「未決の論点」の段落を削除し、決定内容（発行元はこの spec）
    を反映した記述に置き換える
  - 空の `state` や照合に失敗した折り返しが 400 で断られることが試験で示される
  - _Requirements: 9.1, 9.2（登録コードの発行・照合と同じ「第三者が勝手に登録できない」目的）_
  - _Depends: なし_
  - _Boundary: AdminChatIntegration_

- [ ] 9.1 管理画面の受け皿を置き、連携の状態を出す
  - 管理画面の受け皿を置き、**既存の案内の一覧に 3 か所追記する**（分岐・一覧・スマートフォン用の一覧）
  - ペアリングの操作、**サービスごとに何が使えるか**、**連携の状態**を出す
  - **「サービスごとに何が使えるか」は proxy の `capabilities` の応答（`CapabilityReport`）を
    そのまま出す。** 独自に「このサービスはスラッシュコマンドが使える」等を決め打ちしないこと
    ——proxy 側は現状すべてのサービスで `slashCommand: 'none'` を返す（呼びかけでのみ起動する。
    chat-integration-proxy task 12.2）
  - **暗号化の鍵が未設定のときはその旨を出し、ペアリングを始められないようにする**
  - 画面が出て、使える機能と連携の状態が読めることが試験で示される
  - _Requirements: 1.3, 1.4, 12.5_
  - _Depends: 7.3_
  - _Boundary: AdminChatIntegration_

- [ ] 9.2 設定を保存して proxy へ押し込む
  - チャンネル権限の設定を保存する。**押し込みは版を 1 増やして送る**
  - **権限の行と版は 1 つのトランザクションで書く**
    （版だけ進むと、proxy が古い設定を新しいものとして受け取る）
  - 押し込みが失敗しても、proxy が 3.5 の口へ取りに来るので取りこぼしは埋まる
  - 設定を変えると次の実行から反映されることが試験で示される
  - _Requirements: 11.1, 11.2, 11.4_
  - _Depends: 9.1, 3.5_
  - _Boundary: AdminChatIntegration_

- [ ] 9.3 通知の宛先を選び、重なりを知らせる
  - チャンネルの一覧を proxy から取り、**識別子で選ばせる**（名前で選ばせない）
  - **Gen 1 と Gen 2 の両方の宛先に同じチャンネルがあれば、設定の時点で示す**。
    突き合わせにはチャンネル名を使い、一覧を引いたときに保存し直す
  - 保存時に宛先を選ぶ画面でも、識別子で選べるようにする
  - 同じチャンネルが両方の宛先になっているときに注意が出ることが試験で示される
  - _Requirements: 2.2, 12.4_
  - _Depends: 9.2_
  - _Boundary: AdminChatIntegration_

- [ ] 10. 通しで動くことを確かめる
- [ ] 10.1 権限の絞り込みが既定の設定のまま働くことを確かめる
  - **紐付いていない利用者の検索が、誰でも閲覧できるページだけを返すこと。**
    設定を触らず既定のまま走らせる（既定では既存の絞り込みが効かないので、
    設定を変えて通す試験は意味を持たない）
  - 特定ユーザー限定・グループ限定・リンク限定のページが、それぞれ正しく落ちること
  - **ログインしていない相手に何も見せない設定では、公開ページも返らないこと**
  - _Requirements: 3.6, 3.7_
  - _Depends: 5.1_

- [ ] 10.2 ペアリングから通知までを通しで確かめる
  - 登録コードを貼って申し込み、所有確認に答えて成立するまで
  - 設定を保存して押し込まれ、次のコマンドから反映されること
  - 通知が指定のチャンネルへ届き、**1 つの宛先だけ失敗させてやり直すと二重に投稿されない**こと
  - **解除して繋ぎ直すと、紐付けが引き継がれること**
  - _Requirements: 2.1, 2.4, 9.1, 9.5, 9.7, 11.4_
  - _Depends: 9.3, 8.2_

- [ ] 10.3 紐付けと監査の記録を通しで確かめる
  - 紐付いていない利用者が書き込みを打つと、**どの GROWI に対する紐付けが必要か分かる形で**案内が返ること
  - 一度きりのリンクを開いて承認すると紐付きが成立し、同じリンクが 2 度使えないこと
  - 書き込みの後に監査の記録が 1 件でき、**操作した人と発言元のチャンネルが残る**こと
  - 停止した利用者と読み取り専用の利用者が、それぞれ正しく断られること
  - _Requirements: 4.3, 4.4, 7.3, 7.6_
  - _Depends: 6.2, 5.3_

---

## Implementation Notes

- **task 1.3 の暗号化仕組み（`server/keys/key-encryption.ts`）**:
  - 暗号化に使う鍵の入れ替え（世代を進めて古い行を入れ直す移行）を持つタスクが tasks.md に無い。
    design.md の決定表は「古い鍵で復号して新しい鍵で入れ直す移行を1本用意する」を求めているが、
    今実装したのは「行に世代を記録する」までで、実際の移行スクリプトは未着手。
    **feature 全体の検証（`/kiro-validate-impl`）でこの空白を拾うこと。**
  - `withDecryptedChatKey` は現在の世代の鍵1本だけで復号する。旧世代の鍵を渡す手段がまだ無いため、
    上記の移行が実装されるまでは「入れ替えの最中に読めない行が出ない」という design.md の目標に届いていない。
  - 世代の値を読む環境変数 `CHAT_INTEGRATION_KEY_ENCRYPTION_KEY_GENERATION` が新設されたが、
    design.md の決定表にはまだ載っていない。**design.md 側への反映が必要。**
  - task 9.1（管理画面）は、暗号化鍵が「未設定」な場合と「世代の値が不正」な場合を区別して表示すること。
    `isChatKeyEncryptionConfigured` は両方とも `false` を返すため、画面側で分けないと運用者が原因に気づけない。
  - `chat_integration_keys.key` の検査関数は `save`/`create` 経由の書き込みでしか効かない
    （`updateOne` 系は Mongoose が `this` を Query に束縛するため `side` を読めない）。
    task 3.1（KeyStore）は自分側の鍵の書き込みに必ず `save`/`create` を使うこと。
    `chat_pending_pairings.ownKeyPair` 側の検査関数は無条件のため両方の書き込み経路で効く。
- **task 2.1 の実装で判明した、口の絞り込み方の食い違い（task 2.2 が引き継ぐこと）**:
  - task 2.1 は `app.use(CHAT_INTEGRATION_PEER_PREFIX, ...)` という区切り単位でパスを絞る形にした。
    これだと `/…/peer` と `/…/peer/…` だけが掛かり、`/…/peering` のような紛らわしい隣接パスは掛からない。
  - design.md に書かれている task 2.2 の掛け方（`req.path.startsWith(CHAT_INTEGRATION_PEER_PREFIX)`）と、
    `avoid-session-routes.js` に足す正規表現（`/^\/_api\/v3\/chat-integration\/peer\//`、末尾スラッシュ必須）は、
    どちらもこの区切り単位の絞り込みより緩いか厳しいかが食い違っている
    （`startsWith` は `/peering` まで含めてしまい、正規表現は末尾スラッシュが無い `/peer` 単体を取りこぼす）。
  - **task 2.2 は、この 2 か所も `app.use` と同じ区切り単位の判定に揃えること。**
    `CHAT_INTEGRATION_PEER_PREFIX` は task 2.1 で `features/chat-integration/server/consts.ts` に
    切り出し済みなので、そこから import して使う。
  - task 3.2（SignatureGuard）は、口に届いた本文が JSON でないときの判定を
    「本文が空かどうか」ではなく **`Buffer.isBuffer(req.body)`** で行うこと。
    `application/x-www-form-urlencoded` は `express.raw` に掴まれずに全体設定の
    `bodyParser.urlencoded` まで通ってしまうため、**空ではない別の object** になる。
- **task 2.2 の実装で判明した、design.md の誤り（design.md 側への反映が必要）**:
  - design.md 738行「CSRF は障害にならない（確認済み）」という囲みは見落としがある。
    `csurf` は `cookie: false` のとき秘密の置き場を `req.session` から取る仕組みなので、
    session が無いと **`ignoreMethods` を見る手前で要求を落とす**
    （`csurf@1.11.0/index.js` の `verifyConfiguration` 287-291行 → 69行）。
    「`ignoreMethods` に POST が入っているから実質無効」という筋道だけでは足りない。
  - `passport.session()` も `req.session` が無いと同じ理由で要求を落とす
    （`passport@0.6.0/lib/strategies/session.js` の `authenticate` 冒頭）。
  - **このため task 2.2 では、session の除外と同じ区切り単位の判定で `csurf` と
    `passport.session()` の両方も `/peer` の下から外している**（元の位置は動かしていない）。
    これをしないと `/peer` への要求が全て 500 になる（実測済み）。
  - csurf を外したことで実際に検証が無くなるのは **PATCH だけ**である
    （`ignoreMethods` が他の方式を全て並べているため）。`/_api` の `CertifyOrigin` は
    包まずそのまま残しており、design.md は `/peer` の口を全て POST と決めているので、
    当面は問題にならない。**将来 `/peer` の下に PATCH の口を足すときは、ここを読み直すこと。**
  - design.md 709行のコード例（`req.path.startsWith(...)`）と、session の節にある
    正規表現（`/^\/_api\/v3\/chat-integration\/peer\//`、末尾スラッシュ必須）は、
    どちらも task 2.1/2.2 で区切り単位の判定に置き換わっている。**design.md 側の書き換えが必要。**
- **task 3.2 の実装で判明した、`@growi/chat` の `verify()` に内蔵された挙動（feature 全体の検証で踏まえること）**:
  - 封筒の突き合わせ（`acceptEnvelope`）で断るとき、使い捨ての値（nonce）は既に消費済みになっている。
    これは `@growi/chat` の `verify()` 自身が署名の照合の最後に `consumeNonce` を呼ぶ設計から来ており、
    この feature 側の選択ではない。task 3.2 の「検証に成功した後にだけ消費する」は
    「署名の検証に成功した後」という意味で満たされている（封筒の突き合わせは別の関門）。
  - 結果として、正しく署名されたバイト列を封筒不一致で断った後に**同じバイト列を再送すると
    「replayed」として断られる**（望ましい挙動）。
  - task 3.5（routes）は、各口で `req.chatPeer.body` を読み、`req.body` を再度 `JSON.parse` しないこと
    （signature-guard が既に契約側の検査関数を通した型付きの本体を渡している）。
- **task 3.3 の実装で判明した、design.md との食い違い（後続タスクが引き継ぐこと）**:
  - `ResolvedActor.user` は design.md の記す `IUser | null` ではなく **`HydratedDocument<IUser> | null`**
    にした（`IUser` 自体に `_id` が無く、実際の消費者（検索・ページ作成）は `_id` を必要とするため。
    `HydratedDocument<IUser>` は `IUser` の上位互換であり、design.md の記述と矛盾しない）。
    **design.md 側のインターフェース記述も合わせて直すこと。**
  - design.md の post-condition だけでは表現できない箇所を埋めるため、**`resolveReadDenial`
    という関数を新設した**（`resolveActor` とは別の、呼び出し側が使う純粋関数）。
    **task 5.1（CommandEndpoint）は `resolveActor` に加えて
    `resolveReadDenial(resolved, crowi.aclService.isGuestAllowedToRead())` も呼び、
    非 null を返したら読み取りも断ること。** これを呼ばないと、ログインしていない相手に
    何も見せない設定の GROWI で、紐付いていない利用者の検索やリンク展開が素通りしてしまう
    （design.md が3段落を割いている閉じた GROWI の情報漏えいがそのまま残る）。
  - `resolveActor` は未解決の利用者に対して `userGroups: []` を返す（design.md どおり）。
    **task 4.1 はこれを `searchKeyword` へそのまま渡さないこと。** `server/routes/search.ts` の
    既存の呼び方は「未解決なら `null`」の意味で使っており、`[]` を渡すと意味が変わる
    （Gen 1 の欠陥として design.md の Testing Strategy 項目2が名指ししているのと同じ種類の取り違え）。
- **task 3.4 の実装で判明した、口そのものへの上限（未実装・意図的な範囲外）**:
  - protocol spec は所有確認の口について、`(registrationCode, sourceKey)` ごとの上限に**加えて**、
    「口そのものにも上限を置く」（一度も発行されていない登録コードへの大量アクセス対策）という
    2段目の防御を挙げている。**task 3.4 はこの2段目を実装していない**
    （task 1.2 の `chat_challenge_attempts` にこの用途の項目は無く、task 3.4 の文面もこれを求めていない）。
  - 実害は限定的 — 存在しない登録コードへの推測攻撃も、既存の `(registrationCode, sourceKey)` 単位の
    上限がそのまま掛かるため、無制限ではない。**feature 全体の検証で、この2段目の要否を判断すること。**
- **task 4.3 の実装で判明した、design.md の型名の食い違い（design.md 側への反映が必要）**:
  - design.md 375行が挙げる `HistoryMessage` という型は `@growi/chat` に存在しない。
    実際の型名は **`KeepMessage`**（`{ postedAt: string; author: ChatAccountRef; markdown: string }`、
    `packages/chat/src/contract/command.ts`）。design.md 側の型名を直すこと。
  - 投稿者の一括解決は design.md の言う「`$in` で1回引く」ではなく、
    `{ platform, accountId }` の組ごとの `$or` にした（`accountId` は単独では一意でなく、
    `platform` と組み合わせた複合キーの一部なので、素の `$in` では跨サービスの取り違えが起きうる）。
    「まとめて1回引く」という制約自体は満たしている。
- **task 4.4 の実装で判明した、閉じた GROWI × 誰でも見られるページの組み合わせ**:
  - `LinkPreviewMapper` の完全な要約は、**「ページ自体が誰でも見られる」かつ「GROWI がログインしていない相手にも見せる設定」の両方が真のときだけ**出す（`&&`。片方が真でも駄目）。
    design.md 242-247行はこの組み合わせに例外を設けていないため、GRANT_PUBLIC のページでも
    閉じた GROWI ではパスだけになる。
  - リンクの要約の本文抜粋は **2000文字**で切っている。design.md はこの長さを
    `NotificationContent`（task 4.5）向けにしか明記しておらず、**この値は類推による仮の値**。
    task 4.5 の実装時に、両者の長さを揃えるか、共有の定数に切り出すかを検討すること。
- **task 5.1 の実装で新設した、task 6.1 が再利用すべき共有部品**:
  - `server/account-link/create-link-order.ts` の `findOrCreatePendingAccountLinkOrder` は、
    要件7.6の「紐付けが必要」案内（task 5.1 が使う）と、**task 6.1（account-link-start）が
    要求する「保留中の申し込みがあれば使い回し、再送のたびに新しいリンクを作らない」**の
    両方に使う共通の定義。task 5.1 の tasks.md 上の `_Depends:_` には出てこないが、
    **task 6.1 はこのファイルを再利用すること。同じロジックを書き直さない。**
    使い回し判定の単体試験は `create-link-order.spec.ts` にある。
