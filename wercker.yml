box: node:12-slim

test:
  steps:
    - script:
      code: echo "CI processes are migrated to GitHub Actions"


build-prod:
  steps:
    - script:
      code: echo "CI processes are migrated to GitHub Actions"


build-dev:
  steps:
    - script:
      code: echo "CI processes are migrated to GitHub Actions"
