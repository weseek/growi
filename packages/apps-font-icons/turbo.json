{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["svg/**"],
      "outputMode": "new-only"
    }
  }
}
