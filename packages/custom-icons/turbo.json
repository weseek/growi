{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["svg/**"],
      "outputLogs": "new-only"
    },
    "dev": {
      "outputs": ["dist/**"],
      "inputs": ["svg/**"],
      "outputLogs": "new-only"
    }
  }
}
