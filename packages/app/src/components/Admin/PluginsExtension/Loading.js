import {
  Spinner,
} from 'reactstrap';

const Loading = () => {
  return (
    <Spinner className='d-flex justify-content-center aligh-items-center'>
      Loading...
    </Spinner>
  );
};

export default Loading;
